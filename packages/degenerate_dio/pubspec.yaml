name: degenerate_dio
description: Dio HTTP adapter for degenerate-generated OpenAPI Dart clients.
version: 0.4.2
repository: https://github.com/blopker/degenerate
resolution: workspace

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.8.0

dependencies:
  degenerate_runtime: ^0.4.0
  dio: ^5.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
